{
  "name": "@infra/d1",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "migrate:create": "wrangler d1 migrations create starter-db",
    "migrate:local": "wrangler d1 migrations apply starter-db --local --persist-to ../../.wrangler/state",
    "migrate:remote": "wrangler d1 migrations apply starter-db --remote --env production",
    "seed:local": "wrangler d1 execute starter-db --local --persist-to ../../.wrangler/state --file=./seeds/0001_seed.sql",
    "seed:remote": "wrangler d1 execute starter-db --remote --env production --file=./seeds/0001_seed.sql",
    "tables:local": "wrangler d1 execute starter-db --local --persist-to ../../.wrangler/state --command \"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\""
  },
  "devDependencies": {
    "wrangler": "4.105.0"
  }
}
