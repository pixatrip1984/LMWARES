{
  "name": "@workers/admin-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8888 --persist-to ../../.wrangler/state",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --test src/routes/*.test.mjs",
    "cf-typegen": "wrangler types",
    "lint": "echo \"(admin-api) no lint configured\""
  },
  "dependencies": {
    "@starter/config": "0.0.0",
    "@starter/db": "0.0.0",
    "@starter/domain": "0.0.0",
    "@starter/validation": "0.0.0",
    "hono": "4.13.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260627.1",
    "typescript": "~5.9.3",
    "wrangler": "4.105.0"
  }
}
